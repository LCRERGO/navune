// Package constraints holds Navune's always-on constraint suite: tests that
// assert the invariants documented in the ADRs hold when Navune analyzes its
// own repository (ADR 0013 revision 1). It exercises no target project and
// changes no behavior.
package constraints
